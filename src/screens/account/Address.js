import React from 'react';
import { View, Text } from 'react-native';
import { layoutStyle } from '../../styles';

const Address = () => {
  return (
    <View style={layoutStyle.containerPrimary}>
      <Text>estamos en Address</Text>
    </View>
  );
};

export default Address;
